/** Name of the default channel every workspace gets; it cannot be left or deleted. */
export const DEFAULT_CHANNEL_NAME = 'general';

/** Max members in a group DM (Slack parity). */
export const MAX_CONVERSATION_MEMBERS = 9;

/** Server-enforced upload limit in bytes (25MB). */
export const MAX_UPLOAD_BYTES = 25 * 1024 * 1024;

/** Messages per page for cursor pagination. */
export const MESSAGES_PAGE_SIZE = 50;
